'use strict';

//const Categories = require('./categories-schema');

class Categories {

  constructor() {
    this.name = name,
    this.type = type
  }

  get(_id) {
  }
  
  post(record) {
  }

  put(_id, record) {
  }

  delete(_id) {
  }

}

module.exports = Categories;
